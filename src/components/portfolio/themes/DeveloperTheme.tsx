"use client";
import type { ThemeProps } from "./theme-types";
import CompleteThemeSections from "./CompleteThemeSections";
export default function DeveloperTheme({ data }: ThemeProps) { const personal = data.personalInfo ?? data.personal ?? {}; const name = personal.fullName || personal.name || "My Portfolio"; return <main className="min-h-screen bg-black text-white"><header className="border-b border-zinc-800"><div className="mx-auto max-w-6xl px-6 py-24"><p className="font-mono text-sm uppercase tracking-[0.3em] text-cyan-400">~/portfolio</p><h1 className="mt-4 text-5xl font-extrabold md:text-7xl">{name}</h1><p className="mt-6 font-mono text-2xl text-zinc-300">{data.professionalProfile?.title || "Professional"}</p><p className="mt-3 font-mono text-cyan-400">{data.professionalProfile?.domain}</p></div></header><CompleteThemeSections data={data} variant="developer" /><footer className="border-t border-zinc-800 px-6 py-8 text-center font-mono text-sm text-zinc-500">© {new Date().getFullYear()} {name}</footer></main>; }
